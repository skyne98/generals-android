# Default options for the Android port.
# Mirrors ios/config/Options.ini: seed full detail instead of the 2003 auto-detect
# (which drops unknown mobile GPUs to Low). Seeded into the user-data dir on first
# launch by the filesystem bootstrap in SDL3Main.cpp.
#
# GeneralsX-Android @build generals-android 11/07/2026

[Options]
TextureReduction = 0
MaxShellTextures = 16
UseCloud = no
